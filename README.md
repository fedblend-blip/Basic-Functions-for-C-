# Basic-Functions-for-C-
This repository contains basic functions that simplify programming in C++. Functions such as `print` and `delay` are included, as well as functions for resetting the color in the `termcolor` library (the `resetc` function).
